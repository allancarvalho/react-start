import React from "react";
import { Link } from "all-in-one";

const Home = () => (
  <div>
    <h1>Hello Home</h1>
    <Link to="/sobre">Ir para a página sobre \o/</Link>
  </div>
);

export default Home;
